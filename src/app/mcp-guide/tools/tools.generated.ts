// AUTO-GENERATED by pf-app/scripts/generate-mcp-tool-catalog.mjs
// Source-of-truth: pf-app/mcp-server/register-tools-pg.ts
// Do not edit by hand — re-run `node scripts/generate-mcp-tool-catalog.mjs`.

export type ToolCategory =
  | "read"
  | "write"
  | "preview"
  | "execute"
  | "destructive"
  | "analyze";

export type ToolTransport = "http" | "both";

export interface CatalogTool {
  name: string;
  description: string;
  blurb: string;
  category: ToolCategory;
  readOnly: boolean;
  destructive: boolean;
  idempotent: boolean;
  requiresWriteScope: boolean;
  transport: ToolTransport;
  deprecated: boolean;
}

export const TOOL_COUNT_HTTP = 92 as const;
export const TOOL_COUNT_STDIO = 94 as const;

export const TOOLS: ReadonlyArray<CatalogTool> = [
  {
    name: "get_account_balances",
    description: "Get current balances for all accounts. Each account's balance is in its own (account) currency. When reportingCurrency is set, also returns a unified total converted to that currency. Default reporting = user's display currency.",
    blurb: "Get current balances for all accounts.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "search_transactions",
    description: "Flexible transaction search with partial payee match, amount range, date range, category, and tags. Each row carries both entered (user-typed) and account (settlement) amounts; pass reportingCurrency to also include a converted reporting amount per row. For dedup workflows on blank-payee imports, pass `account_id` (FK fast-path) — a year of activity in one account easily exceeds the default 50-row limit, so raise `limit` accordingly. Each row includes `quantity` (nullable; positive for buys, negative for sells; null for cash-proxy and non-investment transactions).",
    blurb: "Flexible transaction search with partial payee match, amount range, date range, category, and tags.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "get_budget_summary",
    description: "Get budget vs actual spending for a specific month. Amounts are in the user's display currency (default reporting); pass reportingCurrency to override.",
    blurb: "Get budget vs actual spending for a specific month.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "get_spending_trends",
    description: "Get spending trends over time grouped by category. Totals are in the user's display currency by default; pass reportingCurrency to override. Issue #210: `priorMonths: N` returns N+1 monthly buckets — the current (partial) month plus N priors. `months` is accepted as a deprecated alias.",
    blurb: "Get spending trends over time grouped by category.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "get_income_statement",
    description: "Generate income statement for a period. Totals are in the user's display currency by default; pass reportingCurrency to override.",
    blurb: "Generate income statement for a period.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "get_net_worth",
    description: "Net worth across all accounts. Returns per-currency assets/liabilities/net AND a unified total in the reporting currency (defaults to user's display currency). Pass `priorMonths` > 0 for a month-by-month trend (returns the current month plus N priors, with `currentMonth` flagged separately); omit for current totals only. Issue #210: legacy `months` param is accepted as a deprecated alias.",
    blurb: "Net worth across all accounts.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "get_goals",
    description: "Get all financial goals with progress. Each goal carries `accountIds: number[]` (every linked account) and `accounts: string[]` (decrypted display names) — issue #130 multi-account linking. Numeric progress fields (issue #233): `currentAmount` (in goal currency, summed across linked accounts using market value for investment accounts and SUM(amount) for cash accounts, FX-converted into goal currency), `progress` and `percentComplete` (0..100, 1dp), `remaining`, `monthlyNeeded` (when a deadline is set).",
    blurb: "Get all financial goals with progress.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "get_categories",
    description: "List all available transaction categories",
    blurb: "List all available transaction categories",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "get_loans",
    description: "[DEPRECATED — use list_loans] Get all loans with amortization summary. Returned in the unified `{ success: true, data: [...] }` envelope as of v3.1.0.",
    blurb: "Get all loans with amortization summary.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: true,
  },
  {
    name: "get_subscription_summary",
    description: "Get all tracked subscriptions with total monthly cost and upcoming renewals. Each subscription's amount is in its own currency; totals are converted to reportingCurrency (defaults to user's display currency).",
    blurb: "Get all tracked subscriptions with total monthly cost and upcoming renewals.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "get_recurring_transactions",
    description: "Get detected recurring transactions (subscriptions, bills, salary). Average amounts are converted to reportingCurrency (defaults to user's display currency) so cross-currency recurring payments aggregate sensibly. Issue #210 — sign convention: `avgAmount` is always POSITIVE; the `direction` field carries the inflow/outflow semantic. Matches the storage convention used by `subscriptions.amount` / `add_subscription` / `list_subscriptions`. (Pre-issue-#210 callers that read raw `avgAmount` to decide sign should switch to `direction`.) Issue #235 — also surfaces `daysSinceLast`, `expectedCadenceDays`, and `flagged` per row (flagged when `daysSinceLast > expectedCadenceDays * ${STALENESS_THRESHOLD_MULTIPLIER}`) so callers can spot stale recurrences.",
    blurb: "Get detected recurring transactions (subscriptions, bills, salary).",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "get_financial_health_score",
    description: "Calculate a financial health score 0-100 with breakdown by component. Component scores are currency-independent ratios; the underlying totals (income, expenses, liabilities, liquid assets) are converted to reportingCurrency (defaults to user's display currency). Issue #235 changes: (a) final score is summed from un-rounded sub-components and rounded once at the end (no off-by-one); (b) liquid assets exclude illiquid asset accounts (uses `accounts.is_investment` + a cash-group whitelist); (c) Net Worth Trend is a real 3M delta with `{ direction, magnitudePct, descriptor }` payload; (d) when there are no budgets the Budget Adherence component is EXCLUDED (not penalized at 50/100) and the remaining weights renormalize — `excludedComponents` lists what was dropped; (e) DTI uses trailing-12-month debt payments / trailing-12-month income (no 3m × 4 extrapolation).",
    blurb: "Calculate a financial health score 0-100 with breakdown by component.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "get_spending_anomalies",
    description: "Find spending categories with >30% deviation from their 3-month average. Totals are converted to reportingCurrency (defaults to user's display currency) so cross-currency spending compares apples-to-apples.",
    blurb: "Find spending categories with >30% deviation from their 3-month average.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "get_spotlight_items",
    description: "Get current attention items — overspent budgets, upcoming bills, uncategorized transactions",
    blurb: "Get current attention items — overspent budgets, upcoming bills, uncategorized transactions",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "get_weekly_recap",
    description: "Get a weekly financial recap: spending summary, income, net cash flow, notable transactions. Totals are converted to reportingCurrency (defaults to user's display currency).",
    blurb: "Get a weekly financial recap: spending summary, income, net cash flow, notable transactions.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "get_cash_flow_forecast",
    description: "Project cash flow for the next 30, 60, or 90 days based on recurring transactions. All balances and event amounts are converted to reportingCurrency (defaults to user's display currency). Issue #210: by default, `currentBalance` scopes to accounts in the 'Banks' or 'Cash Accounts' groups; the response surfaces `accountsIncluded` + `accountsExcluded` so callers see exactly which accounts are in scope. Pass `accountFilter` to override (include/exclude lists or set `includeInvestments: true` to fold investment-account cash sleeves into the projection). Issue #235: response now includes `recurringContributions[]` with one row per detected-or-dropped candidate (`{ name, monthly, daysSinceLast, included, dropReason? }`); empty list explains a near-zero forecast. Stale recurrences (`daysSinceLast > 1.5 × cadence`) are dropped with `dropReason: 'stale'` rather than projected forward.",
    blurb: "Project cash flow for the next 30, 60, or 90 days based on recurring transactions.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "set_budget",
    description: "Set or update a budget for a category in a specific month",
    blurb: "Set or update a budget for a category in a specific month",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "add_goal",
    description: "Create a new financial goal. `account_ids` (issue #130) accepts 0..N account ids — the goal's progress sums across all linked accounts. Legacy single `account` (fuzzy-matched name) is still accepted as a single-element list. Pass `account_ids: []` for a manual-tracking goal.",
    blurb: "Create a new financial goal. `account_ids` (issue #130) accepts 0..N account ids — the goal's progress sums across all linked accounts.",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: false,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "add_account",
    description: "Create a new financial account (bank, investment, credit card, etc.)",
    blurb: "Create a new financial account (bank, investment, credit card, etc.)",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: false,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "record_transaction",
    description: "Record a transaction. Prefer `account_id` (exact, no ambiguity) over `account` name; pass at least one. When only `account` is given, exact/alias/startsWith hits route immediately and weak substring fallbacks are REJECTED with a 'did you mean…' error rather than silently writing to the wrong account. Category auto-detected from payee rules/history when omitted. Investment accounts use a keyword pass (dividend/interest/forex/disbursement) plus a 'Transfers'/'Investment Activity' fallback, and default to uncategorized when none match — never a generic expense category. For cross-currency entries (user typed an amount in a currency that differs from the account's), pass enteredAmount + enteredCurrency and the server locks the FX rate at the transaction date. For stock/ETF/crypto rows pass `quantity` (positive=shares acquired, negative=shares sold) so the holding's share count moves; without it the row is treated as cash-only. Pass `dryRun: true` to validate + resolve without writing — the response shape includes `dryRun: true`, `wouldBeId: null`, and the same resolved* fields a real write returns, so callers can preview routing before committing.",
    blurb: "Record a transaction.",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: false,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "bulk_record_transactions",
    description: "Record multiple transactions at once. Prefer per-row `account_id` (or top-level `account_id` as a fallback for every row that omits its own) over `account` name — exact ids skip fuzzy matching. When only `account` is given, exact/alias/startsWith hits route immediately and weak substring fallbacks fail that row with a 'did you mean…' message rather than silently writing to the wrong account. Category auto-detected when omitted; investment-account rows use a keyword pass (dividend/interest/forex/disbursement) plus a 'Transfers'/'Investment Activity' fallback, and default to uncategorized when none match — never a generic expense category. For cross-currency entries pass enteredAmount + enteredCurrency on each item — the server locks the FX rate at the date. For stock/ETF/crypto rows pass `quantity` per row (positive=shares acquired, negative=shares sold) so the holding's share count moves; without it the row is treated as cash-only. Each per-row result includes `resolvedAccount` so callers can verify routing immediately. Pass top-level `dryRun: true` to validate + resolve every row without writing — each per-row result then carries `dryRun: true` and `wouldBeId: null` alongside the same resolved* fields a real write returns; the response top-level `dryRun: true` distinguishes preview from a real batch. Pass top-level `idempotencyKey` (UUID v4) to make this batch safe to retry — if the same `(user, key)` was already committed within 72h, the original result is returned verbatim with no INSERTs (set per-call: callers should generate one fresh UUID per logical batch). After a successful (non-dryRun) batch the response includes a top-level `possibleDuplicates` array — hints only, never blocks the insert — flagging newly-inserted rows that look suspiciously like an existing row in the same account (same direction, amount within 5%, dates within 7 days). Empty array when nothing matches; never null. Use these to decide whether to delete a leg.",
    blurb: "Record multiple transactions at once.",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: false,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "update_transaction",
    description: "Update fields of an existing transaction by ID. Pass enteredAmount + enteredCurrency to re-lock a cross-currency rate (rare); passing just `amount` keeps the entered side unchanged. To backfill a share count on an existing portfolio row, pass `quantity` (positive=buy/long, negative=sell, or null to clear).",
    blurb: "Update fields of an existing transaction by ID.",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "delete_transaction",
    description: "Permanently delete a transaction by ID",
    blurb: "Permanently delete a transaction by ID",
    category: "destructive",
    readOnly: false,
    destructive: true,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "record_transfer",
    description: "Record a transfer between two of the user's accounts. Creates BOTH legs (debit on source, credit on destination) atomically with a shared link_id so they show up as a paired transfer in the UI. PREFER `from_account_id` / `to_account_id` (exact, no ambiguity) over name; pass at least the name when ids aren't known. When only the names are given, exact/alias/startsWith hits route immediately and weak substring fallbacks are REJECTED with a 'did you mean…' error rather than silently routing the pair to the wrong account. Auto-creates a Transfer category (type='R') if missing. Supports cash transfers, cross-currency transfers (pass `receivedAmount` to lock the bank's landed amount), and in-kind/holding transfers (pass `holding` + `quantity` to move shares between brokerage accounts; `amount` may be 0 for pure in-kind moves). BOTH the source and destination holdings MUST already exist in their respective accounts. Investment accounts require a cash holding for the transaction currency — if a destination cash sleeve is missing, call `add_portfolio_holding` first with the transaction currency. For brokerage stock/ETF/crypto buys and sells (cash sleeve ↔ symbol holding inside one brokerage account), prefer `record_trade` — it handles the same-account in-kind dance automatically. SAME-ACCOUNT FOREX (cash sleeve ↔ cash sleeve in different conceptual currencies inside ONE account, e.g. 'Cash - USD' → 'Cash - CAD'): both sleeves inherit the parent account's currency at the schema level, so the conceptual currency lives only in the holding name suffix (`... - XXX` ISO-4217 code). When source and destination resolve to two cash sleeves with divergent suffixes, `receivedAmount` is honored and drives both the destination cash leg's `amount` and the destination `quantity` (cash sleeves track quantity = amount). When the suffixes match (or are missing), `receivedAmount` is ignored as before — pass `destQuantity` explicitly if you need asymmetric in-kind semantics for same-currency cash sleeves.",
    blurb: "Record a transfer between two of the user's accounts.",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: false,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "record_trade",
    description: "Record a stock/ETF/crypto buy or sell in a brokerage account. Wraps record_transfer with the right same-account in-kind dance so the symbol holding's share count + cost basis flow through the portfolio aggregator. BUY: source=cash sleeve in `currency`, destination=symbol holding (must already exist — call `add_portfolio_holding` first if missing). SELL: mirror — source=symbol holding (must already exist), destination=cash sleeve (must already exist for the trade currency — call `add_portfolio_holding` first if missing). Cross-currency trades require `fxRate` (trade_currency → account_currency); the cash sleeve for the trade currency is auto-managed on first use within record_trade itself. Optional `fees` post as a separate expense transaction on the cash sleeve. PREFER `account_id` over `account` name; weak substring matches on the name path are REJECTED with a 'did you mean…' error rather than silently routing the trade to the wrong account. Use this instead of record_transaction for trades — record_transaction loses the holding-pair link and can't move the share count + cost basis atomically.",
    blurb: "Record a stock/ETF/crypto buy or sell in a brokerage account.",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: false,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "update_transfer",
    description: "Update both legs of an existing transfer pair atomically. Identify the pair by linkId OR by either leg's transaction id. Refuses if the targeted rows don't form a clean transfer pair. To (re)bind the in-kind side, pass `holding` + `quantity` together; to clear it (turn the row back into a pure cash transfer), pass `holdingClear: true`. Omit all three to leave the in-kind side untouched.",
    blurb: "Update both legs of an existing transfer pair atomically.",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "delete_transfer",
    description: "Permanently delete BOTH legs of a transfer pair in a single statement. Identify by linkId OR by either leg's id. Refuses if the rows don't form a clean transfer pair — use delete_transaction per-leg for non-symmetric multi-leg imports.",
    blurb: "Permanently delete BOTH legs of a transfer pair in a single statement.",
    category: "destructive",
    readOnly: false,
    destructive: true,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "delete_budget",
    description: "Delete a budget entry for a category/month",
    blurb: "Delete a budget entry for a category/month",
    category: "destructive",
    readOnly: false,
    destructive: true,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "preview_delete_category",
    description: "Preview deletion of a category. Returns the resolved category id/name plus FK row counts (transactions / rules / subscriptions still referencing it) and a confirmationToken for `delete_category`. The execute step refuses if any FK count is non-zero — reassign or delete dependents first via `preview_bulk_categorize` / `execute_bulk_categorize` (transactions), `update_rule` / `delete_rule` (auto-categorize rules), and `update_subscription` (subscriptions).",
    blurb: "Preview deletion of a category.",
    category: "destructive",
    readOnly: true,
    destructive: true,
    idempotent: true,
    requiresWriteScope: false,
    transport: "http",
    deprecated: false,
  },
  {
    name: "delete_category",
    description: "Delete a category. Refuses if any transactions / auto-categorize rules / subscriptions still reference it (use `preview_bulk_categorize` / `execute_bulk_categorize` to reassign first). MUST be preceded by `preview_delete_category` with a matching id — pass that call's `confirmationToken` here verbatim. The token is single-use and expires after 5 minutes.",
    blurb: "Delete a category.",
    category: "destructive",
    readOnly: false,
    destructive: true,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "update_account",
    description: "Update name, group, currency, note, or alias of an account. Pass exactly ONE of `accountId` (preferred, exact) or `account` (name/alias, fuzzy). Supplying both is allowed only when they resolve to the same account — a mismatch fails loud and does NOT update.",
    blurb: "Update name, group, currency, note, or alias of an account.",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "delete_account",
    description: "Delete an account (only if it has no transactions). Pass exactly ONE of `accountId` (preferred, exact) or `account` (name/alias, fuzzy). Supplying both is allowed only when they resolve to the same account — a mismatch fails loud and does NOT delete.",
    blurb: "Delete an account (only if it has no transactions).",
    category: "destructive",
    readOnly: false,
    destructive: true,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "update_goal",
    description: "Update a financial goal's target, deadline, status, or linked accounts. `account_ids` (issue #130) replaces the existing account-link set atomically — pass `[]` to unlink all, or omit to leave links unchanged.",
    blurb: "Update a financial goal's target, deadline, status, or linked accounts. `account_ids` (issue #130) replaces the existing account-link set atomically — pass `[]` to unlink all, or omit to leave links unchanged.",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "delete_goal",
    description: "Delete a financial goal by name",
    blurb: "Delete a financial goal by name",
    category: "destructive",
    readOnly: false,
    destructive: true,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "create_category",
    description: "Create a new transaction category",
    blurb: "Create a new transaction category",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: false,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "create_rule",
    description: "Create an auto-categorization rule for future imports",
    blurb: "Create an auto-categorization rule for future imports",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: false,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "add_snapshot",
    description: "Record a net-worth snapshot for tracking wealth over time",
    blurb: "Record a net-worth snapshot for tracking wealth over time",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: false,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "apply_rules_to_uncategorized",
    description: "Run all active categorization rules against uncategorized transactions",
    blurb: "Run all active categorization rules against uncategorized transactions",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: false,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "finlynq_help",
    description: "Discover available tools, schema, and usage examples",
    blurb: "Discover available tools, schema, and usage examples",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "add_portfolio_holding",
    description: "Create a portfolio holding (a single position like 'VEQT.TO' inside a brokerage account). The import pipeline auto-creates these from CSV/ZIP uploads; this tool is for manually adding a position the user wants to track without an import.",
    blurb: "Create a portfolio holding (a single position like 'VEQT.TO' inside a brokerage account).",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: false,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "update_portfolio_holding",
    description: "Update a portfolio holding's name, symbol, currency, isCrypto, or note. Renames cascade to all transactions automatically because the portfolio aggregators (issue #86) group by FK holdingId, not by display name — two holdings sharing a name across accounts stay distinct rows in get_portfolio_analysis output. NOTE: the legacy `account` parameter is REFUSED (issue #99) — moving a holding to a different account would leave stale `holding_accounts` rows and orphaned account attribution on every historical transaction. To actually move shares between accounts, use record_transfer (in-kind); to re-attribute existing transactions, update them individually.",
    blurb: "Update a portfolio holding's name, symbol, currency, isCrypto, or note.",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "delete_portfolio_holding",
    description: "Delete a portfolio holding. Transactions referencing it survive — the FK is set to NULL automatically (no data loss; they fall back to the orphan-aggregation path until reassigned).",
    blurb: "Delete a portfolio holding.",
    category: "destructive",
    readOnly: false,
    destructive: true,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "get_portfolio_analysis",
    description: "Portfolio holdings with all investment metrics: quantity, cost basis, avg cost, unrealized/realized gain, dividends, total return, % of portfolio. Returns one row per `holdingId` — two holdings sharing a display name across accounts (e.g. VUN.TO in TFSA + RRSP) appear as separate rows. Per-row amounts stay in each holding's native currency; summary aggregates are converted to reportingCurrency (defaults to user's display currency). Pass `symbols` to filter to specific holdings; matching is case-insensitive substring against the row's `name + symbol + account` combination. Within a single entry, ALL whitespace/paren-separated tokens must match (AND); across multiple entries the result is the union (OR). Pass `account_id` (FK fast-path) or `account` (fuzzy name/alias) to scope results to a single account — `account_id` wins when both are passed. The response includes a `warnings` array listing any `symbols` / `account` filter entries that matched zero rows.",
    blurb: "Portfolio holdings with all investment metrics: quantity, cost basis, avg cost, unrealized/realized gain, dividends, total return, % of portfolio.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "get_portfolio_performance",
    description: "Portfolio performance with avg-cost method: realized P&L, dividends, total return, days held per holding. Returns one row per `holdingId` (two holdings sharing a display name come through as separate rows). Per-row amounts stay in each holding's own (account) currency; the response includes the resolved reportingCurrency for context.",
    blurb: "Portfolio performance with avg-cost method: realized P&L, dividends, total return, days held per holding.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "analyze_holding",
    description: "Deep-dive on a single holding: avg cost, realized gain, dividends, days held, full transaction history. Per-row amounts stay in the holding's account currency; aggregates also surface in reportingCurrency (defaults to user's display currency). When `symbol` substring-matches multiple holdings sharing the same name (TFSA + RRSP), pass `holdingId` to scope the analysis to a single position; otherwise the response includes an `ambiguous` array listing every candidate's `{holdingId, name, symbol, account}` and the caller must pick one.",
    blurb: "Deep-dive on a single holding: avg cost, realized gain, dividends, days held, full transaction history.",
    category: "analyze",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "trace_holding_quantity",
    description: "Per-transaction quantity contributions for a single holding, with running sum. Diagnostic tool for investigating quantity discrepancies (e.g. brokerage statement says 79 shares but the aggregator reports 86 — list every contributing leg to find the extra rows). Read-only. JOINs through `holding_accounts` (issue #25) so the rows match what the four portfolio aggregators see; rows whose `(holding_id, account_id)` pair is missing from `holding_accounts` are OMITTED (they're invisible to the aggregators too — surface the gap via the `unjoinedTransactionCount` field). Matches the `analyze_holding` resolution semantics: `holdingId` is preferred when present (bypasses fuzzy matching); when only `symbol` is given and it spans multiple holdings the response surfaces an `ambiguous` candidate list.",
    blurb: "Per-transaction quantity contributions for a single holding, with running sum.",
    category: "analyze",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "get_investment_insights",
    description: "Portfolio-level investment analytics. `mode: 'patterns'` (default) returns contribution frequency, largest positions, diversification score. `mode: 'rebalancing'` suggests BUY/SELL amounts vs `targets`. `mode: 'benchmark'` compares book-value growth vs a reference index. All monetary aggregates are converted to reportingCurrency (defaults to user's display currency) so cross-currency portfolios aggregate sensibly.",
    blurb: "Portfolio-level investment analytics. `mode: 'patterns'` (default) returns contribution frequency, largest positions, diversification score. `mode: 'rebalancing'` suggests BUY/SELL amounts vs `targets`. `mode: 'benchmark'` compares book-value growth vs a reference index.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "list_loans",
    description: "List all loans with balance, rate, payment, payoff date, and linked account",
    blurb: "List all loans with balance, rate, payment, payoff date, and linked account",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "add_loan",
    description: "Create a new loan. All non-optional fields required; payment_amount defaults to calculated monthly payment.",
    blurb: "Create a new loan.",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: false,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "update_loan",
    description: "Update any field of an existing loan by id",
    blurb: "Update any field of an existing loan by id",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "delete_loan",
    description: "Delete a loan by id",
    blurb: "Delete a loan by id",
    category: "destructive",
    readOnly: false,
    destructive: true,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "get_loan_amortization",
    description: "Full amortization schedule for a loan. Returns every payment period with principal/interest/balance. Amounts are in the loan's own currency; the response includes both the loan currency and the resolved reportingCurrency for context.",
    blurb: "Full amortization schedule for a loan.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "get_debt_payoff_plan",
    description: "Compare debt payoff strategies (avalanche vs snowball) across all user loans with an optional extra monthly payment. Loan balances stay in each loan's own currency; the response includes the resolved reportingCurrency for cross-currency context.",
    blurb: "Compare debt payoff strategies (avalanche vs snowball) across all user loans with an optional extra monthly payment.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "get_fx_rate",
    description: "Get the FX rate to convert 1 unit of `from` into `to` on `date`. Cross-rates are computed by triangulation through USD: rate(from,to) = rate_to_usd[from] / rate_to_usd[to]. The lookup checks user overrides first, then the global cache, then Yahoo/CoinGecko, then the most recent cached rate for each currency.",
    blurb: "Get the FX rate to convert 1 unit of `from` into `to` on `date`.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "list_fx_overrides",
    description: "List the user's manual FX rate overrides. Each override pins rate_to_usd for a currency over a date range; lookup uses the most-specific match.",
    blurb: "List the user's manual FX rate overrides.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "set_fx_override",
    description: "Pin a manual FX rate. Accepts the user-friendly pair shape (1 `from` = `rate` `to` on `date`) and stores it as a rate_to_usd entry under fx_overrides. One side of the pair MUST be USD; cross-pair overrides should be entered as two USD-anchored rows.",
    blurb: "Pin a manual FX rate.",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "delete_fx_override",
    description: "Delete a manual FX rate override by id",
    blurb: "Delete a manual FX rate override by id",
    category: "destructive",
    readOnly: false,
    destructive: true,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "convert_amount",
    description: "Convert an amount from one currency to another using triangulated FX rates. Cross-rates go through USD; user overrides win when they cover the requested date.",
    blurb: "Convert an amount from one currency to another using triangulated FX rates.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "list_subscriptions",
    description: "List all subscriptions with full detail (status, next billing, category, account, notes). Issue #210 — `amount` is always positive (the storage convention); a subscription is by definition an outflow.",
    blurb: "List all subscriptions with full detail (status, next billing, category, account, notes).",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "add_subscription",
    description: "Create a new subscription. Issue #210 — `amount` MUST be positive (the storage convention). A subscription is by definition an outflow; the sign is implicit, not in the value.",
    blurb: "Create a new subscription.",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: false,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "update_subscription",
    description: "Update any field of an existing subscription",
    blurb: "Update any field of an existing subscription",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "delete_subscription",
    description: "Permanently delete a subscription by id",
    blurb: "Permanently delete a subscription by id",
    category: "destructive",
    readOnly: false,
    destructive: true,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "list_rules",
    description: "List all auto-categorization rules with their match patterns and target categories",
    blurb: "List all auto-categorization rules with their match patterns and target categories",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "update_rule",
    description: "Update any field of an existing transaction rule",
    blurb: "Update any field of an existing transaction rule",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "delete_rule",
    description: "Delete a transaction rule by id",
    blurb: "Delete a transaction rule by id",
    category: "destructive",
    readOnly: false,
    destructive: true,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "test_rule",
    description: "Dry-run a rule pattern against the user's existing transactions. Decrypts payee/tags in memory when matching. Returns matched rows without writing.",
    blurb: "Dry-run a rule pattern against the user's existing transactions.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "reorder_rules",
    description: "Reorder rules by assigning new priorities. The first id in `ordered_ids` gets the highest priority.",
    blurb: "Reorder rules by assigning new priorities.",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: false,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "suggest_transaction_details",
    description: "Suggest category + tags for a transaction based on rule matches and historical frequency. Decrypts payees in memory when matching history.",
    blurb: "Suggest category + tags for a transaction based on rule matches and historical frequency.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "list_splits",
    description: "List all splits for a transaction. Decrypts note/description/tags in memory when a DEK is available.",
    blurb: "List all splits for a transaction.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "add_split",
    description: "Add a single split to an existing transaction",
    blurb: "Add a single split to an existing transaction",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: false,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "update_split",
    description: "Update fields of an existing split",
    blurb: "Update fields of an existing split",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "delete_split",
    description: "Delete a split by id",
    blurb: "Delete a split by id",
    category: "destructive",
    readOnly: false,
    destructive: true,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "replace_splits",
    description: "Atomically replace all splits on a transaction. Validates the splits sum equals the parent transaction amount (±$0.01).",
    blurb: "Atomically replace all splits on a transaction.",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "preview_bulk_update",
    description: "Preview a bulk update over transactions matching `filter`. Returns affected count, before/after samples, an `unappliedChanges` array, and a confirmationToken (5-min TTL) for execute_bulk_update. Each `unappliedChanges` entry is `{ field, requestedValue, reason }` — `field` is the change key (e.g. \"category\"), `requestedValue` is the value you sent, `reason` explains the failure. `sampleAfter.category` reflects the resolved category display name when `category` (name) resolved. Accepted `changes` keys: category_id, category (name), account_id, date, note, payee, is_business (0/1), quantity (null clears), portfolioHoldingId, portfolioHolding (name/ticker), tags ({ mode: append|replace|remove, value }). Unknown keys fail with a 400.",
    blurb: "Preview a bulk update over transactions matching `filter`.",
    category: "preview",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "execute_bulk_update",
    description: "Commit a bulk update. Must be preceded by preview_bulk_update; the same filter+changes must be passed. Returns `{ updated, unappliedChanges }` where each `unappliedChanges` entry is `{ field, requestedValue, reason }`. Accepted `changes` keys: category_id, category (name), account_id, date, note, payee, is_business (0/1), quantity (null clears), portfolioHoldingId, portfolioHolding (name/ticker), tags. Unknown keys fail. Aborts (no commit) when ALL requested changes failed to resolve.",
    blurb: "Commit a bulk update.",
    category: "execute",
    readOnly: false,
    destructive: false,
    idempotent: false,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "preview_bulk_delete",
    description: "Preview a bulk delete. Returns affected count, sample rows, and a confirmationToken (5-min TTL) for execute_bulk_delete.",
    blurb: "Preview a bulk delete.",
    category: "destructive",
    readOnly: true,
    destructive: true,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "execute_bulk_delete",
    description: "Commit a bulk delete. Must be preceded by preview_bulk_delete; the same filter must be passed.",
    blurb: "Commit a bulk delete.",
    category: "destructive",
    readOnly: false,
    destructive: true,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "preview_bulk_categorize",
    description: "Preview a bulk-categorize (shortcut for preview_bulk_update with only category_id set). Returns affected count + sample + confirmationToken.",
    blurb: "Preview a bulk-categorize (shortcut for preview_bulk_update with only category_id set).",
    category: "preview",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "execute_bulk_categorize",
    description: "Commit a bulk-categorize. Must be preceded by preview_bulk_categorize with the same filter + category_id.",
    blurb: "Commit a bulk-categorize.",
    category: "execute",
    readOnly: false,
    destructive: false,
    idempotent: false,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "detect_subscriptions",
    description: "Scan recent transactions (via the decrypted tx cache) and return candidate subscriptions — payees with 3+ regular-cadence occurrences and stable amounts. Returns a confirmationToken for bulk_add_subscriptions. Issue #210 — `avgAmount` on each candidate is always positive (matches the `subscriptions.amount` storage convention).",
    blurb: "Scan recent transactions (via the decrypted tx cache) and return candidate subscriptions — payees with 3+ regular-cadence occurrences and stable amounts.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "bulk_add_subscriptions",
    description: "Commit a set of detected subscriptions. Pass the candidates returned by detect_subscriptions (payee + amount + cadence), plus the confirmationToken.",
    blurb: "Commit a set of detected subscriptions.",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: false,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "list_pending_uploads",
    description: "List MCP uploads that are still pending or previewed (not yet executed, cancelled, or expired).",
    blurb: "List MCP uploads that are still pending or previewed (not yet executed, cancelled, or expired).",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "preview_import",
    description: "Preview an uploaded CSV/OFX/QFX file. Returns first 20 parsed rows, dedup hit count, category auto-match coverage, unresolved accounts, probable cross-source duplicates (FX-spread + ±7 day fuzzy match — heuristic, not exact), and a confirmationToken for execute_import.",
    blurb: "Preview an uploaded CSV/OFX/QFX file.",
    category: "preview",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "execute_import",
    description: "Commit an upload as transactions. Requires the token from preview_import with matching uploadId + templateId + columnMapping.",
    blurb: "Commit an upload as transactions.",
    category: "execute",
    readOnly: false,
    destructive: false,
    idempotent: false,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "cancel_import",
    description: "Cancel a pending MCP upload — marks the row as cancelled and deletes the file from disk.",
    blurb: "Cancel a pending MCP upload — marks the row as cancelled and deletes the file from disk.",
    category: "destructive",
    readOnly: false,
    destructive: false,
    idempotent: false,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "list_staged_imports",
    description: "List the user's staged imports (pending statements awaiting review). Each row carries the import-level metadata + a synthetic `reconciliation` block (statement balance vs current balance vs projected post-approval balance) when the import is bound to an account. Use this before `get_staged_import` to find imports needing review.",
    blurb: "List the user's staged imports (pending statements awaiting review).",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "get_staged_import",
    description: "Fetch full detail for one staged import — top-level metadata + every row with decrypted display fields. Returns 404 (Not found) when the id doesn't belong to the caller; cross-tenant attacks never leak that the id exists for someone else.",
    blurb: "Fetch full detail for one staged import — top-level metadata + every row with decrypted display fields.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "list_staged_transactions",
    description: "Flat list of staged transaction rows across one or many imports. Useful for 'show me every uncategorized pending row across all my pending statements.' Filter by stagedImportId, dedupStatus, rowStatus, or txType. Always user-scoped.",
    blurb: "Flat list of staged transaction rows across one or many imports.",
    category: "read",
    readOnly: true,
    destructive: false,
    idempotent: true,
    requiresWriteScope: false,
    transport: "both",
    deprecated: false,
  },
  {
    name: "update_staged_transaction",
    description: "Edit a single staged transaction row in place. Same shape and validations as the PATCH /api/import/staged/[id]/rows/[rowId] endpoint. Re-encrypts text fields under the row's existing tier (service/user); never flips the tier mid-edit. import_hash is NEVER recomputed (load-bearing for cross-source dedup). peerStagedId and targetAccountId are mutually exclusive.",
    blurb: "Edit a single staged transaction row in place.",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "link_staged_transfer_pair",
    description: "Sugar over update_staged_transaction: pair two staged rows as a transfer. Sets txType='R' on both and cross-points peer_staged_id. Validates: same staged_import_id, opposite-sign amounts (additive inverse), and different account names. Either row id may be passed first.",
    blurb: "Sugar over update_staged_transaction: pair two staged rows as a transfer.",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: false,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "approve_staged_rows",
    description: "Materialize staged rows into the live transactions table. Two-step: first call returns a summary + confirmationToken (5-min TTL); second call with the token + same payload commits. Optional rowIds = subset (omit = all). Optional idempotencyKey is stored 72h to make retries safe. Calls invalidateUser after commit so the per-user tx cache reflects the new rows.",
    blurb: "Materialize staged rows into the live transactions table.",
    category: "write",
    readOnly: false,
    destructive: false,
    idempotent: false,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
  {
    name: "reject_staged_import",
    description: "Reject (hard-delete) a staged import. Two-step: first call returns a summary + confirmationToken (5-min TTL); second call with the token commits. Cascades the staged_transactions delete via FK.",
    blurb: "Reject (hard-delete) a staged import.",
    category: "destructive",
    readOnly: false,
    destructive: true,
    idempotent: true,
    requiresWriteScope: true,
    transport: "both",
    deprecated: false,
  },
];
